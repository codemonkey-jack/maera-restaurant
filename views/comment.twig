{#<?php#}
<div class="blog-comment panel{{ comment.comment_type }}" id="blog-comment-{{ comment.ID }}" itemprop="comment">
	<h5 class="comment-author">{{ comment.author.name }}</h5>
	<div class="comment-content panel-body">{{ comment.comment_content|wpautop }}</div>
</div>
